package CourseProject;

public class Player {

    int id;
    boolean isActive;

    public Player(int id, boolean isActive) {
        this.id = id;
        this.isActive = isActive;
    }
}
